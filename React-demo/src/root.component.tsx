import React from "react";

export default function Root(props) {
  return <section>{props.name} is mounted!
            <h1 style={{color: "coral"}}>Hello React!</h1>
            <hr></hr>
            
  </section>;
}
